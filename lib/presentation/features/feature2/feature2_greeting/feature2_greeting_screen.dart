import 'package:flutter/material.dart';

final class Feature2GreetingScreen extends StatelessWidget {
  const Feature2GreetingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is the Feature2Greeting Screen'),
      ),
    );
  }
}
