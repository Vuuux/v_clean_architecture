import 'package:flutter/material.dart';

final class Feature2WithIdPathScreen extends StatelessWidget {
  const Feature2WithIdPathScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is the Feature2WithIdPath Screen'),
      ),
    );
  }
}
