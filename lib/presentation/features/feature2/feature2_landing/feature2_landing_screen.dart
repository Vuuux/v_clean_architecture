import 'package:flutter/material.dart';

final class Feature2LandingScreen extends StatelessWidget {
  const Feature2LandingScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('This is the Feature2Landing Screen'),
      ),
    );
  }
}
